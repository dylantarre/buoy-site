---
import CompareLayout from '../../layouts/CompareLayout.astro'

const comparison = [
  { aspect: 'Primary Use', eslint: 'Code quality & syntax', buoy: 'Design system drift' },
  { aspect: 'Context Awareness', eslint: 'Code patterns only', buoy: 'Understands design tokens' },
  { aspect: 'Hardcoded Colors', eslint: 'No detection', buoy: 'Full detection + suggestions' },
  { aspect: 'Duplicate Components', eslint: 'No detection', buoy: 'Semantic duplicate finding' },
  { aspect: 'Design Tokens', eslint: 'No understanding', buoy: 'Maps to your tokens' },
  { aspect: 'Adoption Metrics', eslint: 'None', buoy: 'Full coverage stats' },
  { aspect: 'Configuration', eslint: 'Complex rule setup', buoy: 'Works out of the box' },
  { aspect: 'Use Together?', eslint: '—', buoy: 'Yes! Complementary tools' },
]
---

<CompareLayout
  title="Buoy vs ESLint | Design System Context Awareness"
  description="ESLint catches syntax errors. Buoy understands design system context, detecting semantic drift ESLint can't see."
  keywords="eslint design system, eslint alternative, semantic code analysis"
  heroTitle="ESLint for Code Quality, Buoy for Design"
  heroDescription="ESLint catches code quality issues. Buoy catches design system drift. Use both together."
  competitor="ESLint"
>
  <!-- Key Difference -->
  <section class="py-16">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="bg-navy/50 border border-buoy/30 rounded-xl p-8 text-center">
        <h2 class="text-xl font-bold text-light mb-4">The Key Difference</h2>
        <p class="text-lg text-slate">
          ESLint asks: <span class="text-warning">"Is this valid JavaScript?"</span>
        </p>
        <p class="text-lg text-slate mt-2">
          Buoy asks: <span class="text-buoy">"Does this follow your design system?"</span>
        </p>
      </div>
    </div>
  </section>

  <!-- Comparison Table -->
  <section class="py-16 bg-navy">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-2xl font-bold text-light mb-8 text-center">Feature Comparison</h2>

      <div class="bg-navy-dark border border-navy-light/30 rounded-xl overflow-hidden">
        <div class="grid grid-cols-3 bg-navy border-b border-navy-light/20">
          <div class="p-4 font-semibold text-slate">Feature</div>
          <div class="p-4 font-semibold text-slate border-l border-navy-light/20">ESLint</div>
          <div class="p-4 font-semibold text-buoy border-l border-navy-light/20">Buoy</div>
        </div>

        {comparison.map((row, index) => (
          <div class={`grid grid-cols-3 ${index % 2 === 0 ? 'bg-navy/30' : ''}`}>
            <div class="p-4 text-light">{row.aspect}</div>
            <div class="p-4 text-slate border-l border-navy-light/20">{row.eslint}</div>
            <div class="p-4 text-success border-l border-navy-light/20">{row.buoy}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- What ESLint Can't Catch -->
  <section class="py-16">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-2xl font-bold text-light mb-8 text-center">What ESLint Can't Catch</h2>

      <div class="space-y-4">
        <div class="bg-navy/50 border border-navy-light/20 rounded-xl p-4">
          <h3 class="font-semibold text-light mb-2">Hardcoded Colors</h3>
          <pre class="text-sm font-mono bg-navy-dark p-3 rounded"><code class="text-slate">{`// ESLint: ✓ Valid JavaScript
// Buoy: ⚠️ Hardcoded color, use var(--primary)
<div style={{ color: '#3b82f6' }} />`}</code></pre>
        </div>

        <div class="bg-navy/50 border border-navy-light/20 rounded-xl p-4">
          <h3 class="font-semibold text-light mb-2">Duplicate Components</h3>
          <pre class="text-sm font-mono bg-navy-dark p-3 rounded"><code class="text-slate">{`// ESLint: ✓ Valid exports
// Buoy: ⚠️ 4 Button implementations found
export { Button } from './Button'
export { ButtonNew } from './ButtonNew'
export { MyButton } from './MyButton'`}</code></pre>
        </div>

        <div class="bg-navy/50 border border-navy-light/20 rounded-xl p-4">
          <h3 class="font-semibold text-light mb-2">Token Usage</h3>
          <pre class="text-sm font-mono bg-navy-dark p-3 rounded"><code class="text-slate">{`// ESLint: ✓ Valid CSS
// Buoy: ⚠️ Using 16px instead of --spacing-md
padding: 16px;`}</code></pre>
        </div>
      </div>
    </div>
  </section>

  <!-- Use Together -->
  <section class="py-16 bg-navy">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="text-2xl font-bold text-light mb-4">Use Both Together</h2>
      <p class="text-slate mb-8 max-w-2xl mx-auto">
        ESLint and Buoy are complementary. ESLint catches code quality issues.
        Buoy catches design system drift. Run both in your CI pipeline.
      </p>

      <div class="bg-navy-dark border border-navy-light/30 rounded-xl p-4 inline-block">
        <pre class="text-sm font-mono text-left"><code class="text-slate">{`# .github/workflows/ci.yml
- name: Lint
  run: npm run lint          # ESLint

- name: Design Drift Check
  run: npx @buoy-design/cli drift check  # Buoy`}</code></pre>
      </div>
    </div>
  </section>
</CompareLayout>
