---
import DocsLayout from '../../../layouts/DocsLayout.astro'
---

<DocsLayout title="buoy import" description="Import design tokens from external sources">
  <h1>buoy import</h1>

  <p>
    Import design tokens from external files. Supports JSON (DTCG, Tokens Studio,
    Style Dictionary) and CSS formats.
  </p>

  <h2>Usage</h2>

  <pre><code>buoy import &lt;file&gt; [options]</code></pre>

  <h2>Arguments</h2>

  <table>
    <thead>
      <tr>
        <th>Argument</th>
        <th>Description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><code>file</code></td>
        <td>Token file to import (JSON, CSS)</td>
      </tr>
    </tbody>
  </table>

  <h2>Options</h2>

  <table>
    <thead>
      <tr>
        <th>Option</th>
        <th>Description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><code>-o, --output &lt;path&gt;</code></td>
        <td>Output file path (default: design-tokens.css)</td>
      </tr>
      <tr>
        <td><code>-f, --format &lt;format&gt;</code></td>
        <td>Output format: css, json, scss (default: "css")</td>
      </tr>
      <tr>
        <td><code>--dry-run</code></td>
        <td>Preview import without writing files</td>
      </tr>
    </tbody>
  </table>

  <h2>Examples</h2>

  <h3>Import from Tokens Studio export</h3>

  <pre><code>$ buoy import tokens-studio-export.json

Importing tokens...

Found 67 tokens:
  - 24 colors
  - 12 spacing
  - 8 typography
  - 23 other

✓ Created design-tokens.css</code></pre>

  <h3>Import to SCSS</h3>

  <pre><code>$ buoy import tokens.json --format scss -o _tokens.scss

✓ Created _tokens.scss</code></pre>

  <h2>Supported Formats</h2>

  <h3>Input</h3>
  <ul>
    <li><strong>DTCG</strong> - Design Token Community Group format</li>
    <li><strong>Tokens Studio</strong> - Figma Tokens Studio export</li>
    <li><strong>Style Dictionary</strong> - Amazon Style Dictionary format</li>
    <li><strong>CSS</strong> - CSS custom properties</li>
  </ul>

  <h3>Output</h3>
  <ul>
    <li><strong>css</strong> - CSS custom properties</li>
    <li><strong>json</strong> - Standard JSON format</li>
    <li><strong>scss</strong> - SCSS variables</li>
  </ul>

  <h2>Related</h2>

  <ul>
    <li><a href="/docs/cli/tokens">buoy tokens</a> - Generate tokens from code</li>
    <li><a href="/docs/cli/compare">buoy compare</a> - Compare tokens to codebase</li>
    <li><a href="/docs/integrations/figma">Figma Integration</a></li>
  </ul>
</DocsLayout>
